using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class playerController : MonoBehaviour {

    private float moveSpeed = 0.1f;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		//if ((Input.GetAxis("Vertical")) > 0) { transform.position += Vector3.forward;  }
     //   if ((Input.GetAxis("Vertical")) < 0) { transform.position += Vector3.back; }

      //  if ((Input.GetAxis("Horizontal")) > 0) { transform.position += Vector3.right; }
     //   if ((Input.GetAxis("Horizontal")) < 0) { transform.position += Vector3.left; }

    }
}
